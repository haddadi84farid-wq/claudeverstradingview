node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Secrets — never commit (this fork is public)
.env
.env.*
*.pem
*.key

# Regles de trading personnelles (utiliser ~/.kasper/rules.json)
rules.json
